package ${model.pkgName};

import com.thwt.core.annotation.Generated;
import com.thwt.core.service.AbstractServiceOptions;
import com.thwt.core.annotation.Jsonizable;
#foreach($stmt in $model.imports)
import $stmt;
#end
/**
 * auto generated by service code generator, any modification to this file would be overridden !
 *
 */
@Generated
@Jsonizable(category="service", jsonName="${model.targetObject.moduleId}")
public class ${model.name} extends AbstractServiceOptions {
  
#if($model.fields)
#foreach($fld in $model.fields) 
    ${fld.javaStatement}
#end
#end   

#foreach($m in $model.methods) 
#if(!$m.consstructor)
    $m.javaStatement

#end    
#end        

  public void updateFrom(${model.targetObjectClass} target) {
#if($model.fields)
#foreach($fld in $model.fields) 
    this.${fld.name} = target.${fld.getter.name}();
#end
#end   
  }
  
  public void updateTo(${model.targetObjectClass} target) {
#if($model.fields)
#foreach($fld in $model.fields) 
    target.${fld.setter.name}(this.${fld.name});
#end
#end   
  }
}
